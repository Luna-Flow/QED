///|
/// Blackbox test package: full `using` union (main `alias.mbt` is not in scope here).
using @kernel {
  type ConstId,
  type DbTerm,
  type HolType,
  type Term,
  type KernelState,
  type SigError,
  type LogicError,
  bool_ty,
  mk_tyvar,
  mk_var,
  mk_eq,
  empty_kernel_state,
  ks_add_const,
  ks_push_scope,
  assume_checked,
  ks_mk_const,
  ks_mk_const_instance,
  ks_const_schema,
  ks_lookup_const_id,
  mk_comb,
  mk_abs,
  mk_const,
  mk_const_bound,
  type_of,
  is_bool_ty,
  hol_type_to_string,
  dest_fun_ty,
  dest_abs,
  dest_comb,
  dest_const,
  dest_eq,
  dest_var,
  fun_ty,
  to_db_term,
  term_alpha_eq,
  ty_eq,
  ty_is_instance_of,
  db_term_eq,
  ind_ty,
}
